<script>
  let { title = '', hint = '' } = $props();
</script>

<section class="section-title">
  <span>{title}</span>
</section>
<div class="empty">
  <p>{hint}</p>
  <p style="font-size: 12px; margin-top: 12px;">
    The C7 card lands the shell + All-timers page; the {title.toLowerCase()} grid
    arrives in C8 per the card context. Stub here keeps the navigation working.
  </p>
</div>
